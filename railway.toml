[build]
builder = "nixpacks"
buildCommand = "pnpm install --frozen-lockfile && pnpm run prisma-generate && pnpm run build:backend"

[deploy]
startCommand = "NODE_OPTIONS='--max-old-space-size=2048' node --experimental-require-module ./dist/apps/backend/src/main.js"
restartPolicyType = "on_failure"
